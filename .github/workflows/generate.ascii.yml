name: Generate ascii yml
on: 
  push
jobs:
  ascii_job :
    runs-on : ubuntu-latest
    steps:
      - name : Checkout Repo
        uses : actions/checkout@v6
      - name : Executiing shell script
        run : |
          chmod +x ascii-script.sh
          ./ascii-script.sh